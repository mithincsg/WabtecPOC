# --- Parsing -------------------------------------------------------------
PyMuPDF>=1.24        # PDF: text with font attributes, table detection
openpyxl>=3.1        # XLSX/XLSM read (ingestion) and write (datasheet export)
# .py/.pyi use the stdlib `ast`; .html uses the stdlib `html.parser`.

# --- Embeddings ----------------------------------------------------------
sentence-transformers>=3.0   # runs BAAI/bge-m3 in process
transformers>=4.44           # bge-m3 tokenizer, used for chunk token budgets

# --- Retrieval -----------------------------------------------------------
chromadb>=0.5        # dense vector store
rank-bm25>=0.2.2     # keyword arm of hybrid search
numpy>=1.26          # BM25 ranking and confidence cosines (also a chromadb dep)

# --- Backend -------------------------------------------------------------
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9   # required by FastAPI for file uploads
pydantic>=2.7
requests>=2.31       # calls the local Ollama server
pyyaml>=6.0
python-dotenv>=1.0   # loads .env for host/port/CORS/Ollama overrides

# --- Dev / test ----------------------------------------------------------
pytest>=8.0
httpx>=0.27          # FastAPI TestClient
