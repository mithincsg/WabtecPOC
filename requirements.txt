# --- Parsing -------------------------------------------------------------
PyMuPDF>=1.24        # PDF: the parameter guide converter
openpyxl>=3.1        # XLSX/XLSM read (CAF workbook) and write (datasheet export)
# .py/.pyi use the stdlib `ast`; track XML uses the stdlib ElementTree.

# --- Retrieval -----------------------------------------------------------
rank-bm25>=0.2.2     # the app's only retrieval arm
numpy>=1.26          # BM25 ranking

# --- Backend -------------------------------------------------------------
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9   # required by FastAPI for file uploads
pydantic>=2.7
requests>=2.31       # calls the local Ollama server
pyyaml>=6.0
python-dotenv>=1.0   # loads .env for host/port/CORS/Ollama overrides

# --- Dev / test ----------------------------------------------------------
pytest>=8.0
httpx>=0.27          # FastAPI TestClient
